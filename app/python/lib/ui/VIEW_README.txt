A VIEW SHOULD HAVE A FUNCTION CALLED toJSON and update

toJSON builds a JSON object, that is recursively iterated
by the PythonNode Bridging Library.

this converts the JSON to HTML. 

all events get passed from HTML back to the python script.

